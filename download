
### **🚀 Upload Your React Portfolio to GitHub**  

1️⃣ **Open your terminal** and navigate to your project folder:  
   ```bash
   cd /path/to/your/project
   ```

2️⃣ **Initialize Git** (if you haven't already):  
   ```bash
   git init
   ```

3️⃣ **Check ignored files** (optional):  
   ```bash
   git status --ignored
   ```  
   This helps confirm your `.gitignore` is working as expected.

4️⃣ **Stage all files for commit:**  
   ```bash
   git add .
   ```

5️⃣ **Commit the changes:**  
   ```bash
   git commit -m "Initial commit"
   ```

6️⃣ **Create a new GitHub repository** (on GitHub’s website).  
   - Don’t initialize it with a README (GitHub will warn about conflicts).  
   - Copy the repository URL.

7️⃣ **Link your local project to GitHub:**  
   ```bash
   git remote add origin https://github.com/your-username/your-repo-name.git 
   ```

8️⃣ **Push your project to GitHub:**  
   ```bash
   git branch -M main
   git push -u origin main
   ```

---

